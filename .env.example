# 실제 판정을 켤 때 Vercel 환경변수(또는 .env.local)에 설정.
# 프론트로는 절대 노출되지 않는다(서버리스 함수에서만 사용).

ANTHROPIC_API_KEY=sk-ant-...     # 없으면 자동으로 mock 판정
JUDGE_MODEL=claude-sonnet-5      # 모델 교체는 이 변수 하나로
# MOCK_JUDGE=1                    # 키가 있어도 강제로 mock 쓰고 싶을 때
