node_modules
dist
.vercel
.env
.env.local
.DS_Store

__pycache__/
.vite

.claude/settings.local.json

# 프롬프트 콘솔(admin) 편집본 — 로컬 전용 (S8 에서 콘솔과 함께 들어옴)
# 프롬프트 콘솔(admin) 편집본 — 로컬 전용
prompt-overrides.json

# iCloud 동기화 충돌 복사본 — 커밋 금지 (PR #25 리뷰에서 130개 유입 발견)
* 2.*
